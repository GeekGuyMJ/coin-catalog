# Preserved dead .v2 duplicates (2026-08-11): intentionally NOT imported/deployed.
# These are backups only — restore manually if a feature is later found to need them.
app/_backup_dead_v2_files/

# Local backup / cache artifacts — never publish to GitHub Pages.
*.bak
*.bak_*
*.bak-*
*.bak.*
*.cache